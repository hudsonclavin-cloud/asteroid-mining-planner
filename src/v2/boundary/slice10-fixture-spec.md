# Slice 10 Fixture Specification

## Overview

This fixture extends the existing Slice 2 inner-solar-system Horizons truth pattern to the full Slice 10 screening window.

- Bodies: Sun, Mercury, Venus, Earth, Moon, Mars
- Window: 2026-01-01 to 2040-12-31 (inclusive; 5,479 daily records per body)
- Storage location: `tests/fixtures/v2/horizons-inner-solar-system-2026-2040.json`
- Format: same JSON envelope as `tests/fixtures/v2/horizons-inner-system-90d.json`

## Horizons API parameters

Matches the Slice 2 spec and generator pattern:

| Parameter     | Value                    |
|---------------|--------------------------|
| `EPHEM_TYPE`  | `VECTORS`                |
| `REF_SYSTEM`  | `ICRF`                   |
| `REF_PLANE`   | `FRAME`                  |
| `TIME_TYPE`   | `TDB`                    |
| `OUT_UNITS`   | `KM-S`                   |
| `VEC_TABLE`   | `2`                      |
| `START_TIME`  | `2026-01-01`             |
| `STOP_TIME`   | `2040-12-31`             |
| `STEP_SIZE`   | `1d`                     |

### Center IDs per body

| Body    | NAIF `COMMAND` | `CENTER`    | Notes |
|---------|---------------|-------------|-------|
| Sun     | `10`          | `@ssb`      | Preserved from Slice 2 for schema and overlap compatibility |
| Mercury | `199`         | `@sun`      | Heliocentric ICRF J2000 |
| Venus   | `299`         | `@sun`      | Heliocentric ICRF J2000 |
| Earth   | `399`         | `@sun`      | Heliocentric ICRF J2000 |
| Moon    | `301`         | `500@399`   | Preserved from Slice 2 for schema compatibility |
| Mars    | `499`         | `@sun`      | Heliocentric ICRF J2000 |

## Validation

- Overlap gate: exact timestamp coverage for the existing `2026-05-01` through `2026-07-30` window and per-component agreement within `1e-3 km` position and `1e-9 km/s` velocity.
- Fresh-query gate: spot-check against fresh Horizons responses at 10 dates spanning the 2026-2040 window, same tolerance.

## Generator

Generated by:

`tools/slice10-research/extend-horizons-fixture.mjs`
