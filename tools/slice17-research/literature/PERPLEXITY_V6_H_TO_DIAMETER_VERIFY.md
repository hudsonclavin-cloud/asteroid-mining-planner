# V6 — VERIFICATION PASS: H→diameter conversion + albedo uncertainty
# (Aster Slice 17, gates the size-range column copy. NOT exploratory
# research — one-line verdicts with primary-source citations only.)

This is a verification pass, not a research question. For each numbered item
below, give a ONE-LINE verdict with the exact value/formula and a citation
to a PRIMARY source (peer-reviewed paper, NASA/JPL/MPC/IAU documentation —
not blogs, not Wikipedia, not forum posts). Do not add follow-up questions,
do not synthesize recommendations, do not expand scope. If an item cannot be
verified against a primary source, write exactly: UNVERIFIED — and one
clause on what was searched. Never estimate in place of verification.

1. The standard asteroid H→diameter relation. State the exact formula and
   the exact value of the constant (commonly quoted as D(km) =
   1329/sqrt(p_v) × 10^(−H/5)), with the primary source that establishes
   the constant (author/year/document).

2. The provenance of the constant itself: which primary publication derives
   1329 (or the correct value if not 1329), and from what (V-band solar
   magnitude etc.). One line.

3. Standard geometric albedo (p_v) assumption ranges by broad taxonomic
   class, from a primary survey source (e.g., a NEOWISE/Mainzer et al.
   population paper or equivalent): the range for C-complex, S-complex,
   and X-complex (or the classes the primary source actually uses). One
   line per class, each with its citation.

4. The commonly used default albedo when taxonomy is unknown for NEAs, and
   its primary source. One line.

5. The resulting diameter uncertainty factor when H is known but albedo is
   unknown across the plausible p_v range in item 3 (state as "diameter
   uncertain by a factor of ≈X between p_v=a and p_v=b" — arithmetic from
   the verified range, shown explicitly).

6. Typical 1-sigma uncertainty in H itself for NEA catalog values, if a
   primary source states one (survey calibration papers acceptable). If no
   primary source states it plainly: UNVERIFIED.

Output format: numbered list, one line each (item 5 may be two), citation
after each line. Nothing else.
