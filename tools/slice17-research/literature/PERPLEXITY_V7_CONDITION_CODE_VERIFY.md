# V7 — VERIFICATION PASS: orbit condition code (U parameter) bands +
# warning text (Aster Slice 17, gates the orbit-quality column UI copy.
# NOT exploratory research — one-line verdicts with primary-source
# citations only.)

This is a verification pass, not a research question. For each numbered item
below, give a ONE-LINE verdict (item 2 excepted — it is a verbatim table)
with a citation to the PRIMARY source: the Minor Planet Center's own
documentation of the uncertainty parameter U, and/or JPL SSD/SBDB
documentation where it defines or mirrors the parameter. No secondary
sources. If an item cannot be verified against those primary pages, write
exactly: UNVERIFIED — and one clause on what was searched. Do not add
follow-up questions or recommendations.

1. The authoritative definition of the U parameter: what quantity it
   encodes (the in-orbit longitude runoff over what time span), the
   logarithmic form, and which body (MPC) assigns it. One line, cite the
   MPC page.

2. The full 0–9 band threshold table, VERBATIM from the primary source:
   for each U value, the runoff bound (value and unit exactly as the
   source states it). Reproduce the table exactly as published — this is
   the one item where verbatim transcription is required, because Aster's
   UI copy must quote bands only if they match the source exactly.

3. The primary source's own caveat/warning language about what U must NOT
   be used for (e.g., that it is not a predictor of future positional
   uncertainty / not to be over-interpreted). Quote the sentence(s)
   verbatim with the citation. If JPL SBDB carries a distinct warning of
   its own, quote that too, separately attributed.

4. Whether the letters E/D/F (or any non-numeric codes) appear in the
   primary specification alongside 0–9, and what they mean. One line.

5. The as-of/version indication on the primary page if any (last-updated
   date or equivalent), so the verification carries a freshness stamp. One
   line; if the page carries none, say NONE STATED.

Output format: numbered list; items 2 and 3 verbatim with citations; all
others one line each with citation. Nothing else.
