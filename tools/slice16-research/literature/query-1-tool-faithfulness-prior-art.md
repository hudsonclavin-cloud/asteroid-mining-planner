> **Source:** Perplexity Pro (deep research) — Slice 16 Query 1 (prior art & positioning)
> **Date fired:** 2026-07-18 · **Reconstructed:** 2026-07-19
> **PROVENANCE / RECONSTRUCTION NOTE:** this body was recovered from a PDF
> page-save of the Perplexity output (direct paste failed repeatedly);
> text extracted programmatically and re-flowed for readability. TOKEN
> CONTENT PRESERVED IN ORDER; original formatting (headings, bullets,
> citation links) not recoverable from the PDF round-trip. The tool's
> own official-published vs third-party-estimated labels are preserved
> verbatim and must survive any citation.
> **Status:** RAW PRE-RESEARCH — leads, not locked facts. Nothing here
> enters a DEC, the S16 pre-registration, or Appendix A without
> independent verification (verify-before-lock).
> **This file IS the ingestion source for query-1** — no other on-disk
> copy exists.

---

Most existing work evaluates whether agents call the right tools and complete tasks, not whether their final answers faithfully carry through tool outputs, refusals, and provenance. As of July 2026, there is essentially no benchmark that (a) treats structured refusals as first - class outcomes, (b) uses domain - grounded scientific tools with repo - verified truth, and (c) grades explicit propagation of envelopes (values, uncertainty, assumptions, provenance) into answers—so your study would be filling a clearly identifiable gap. 

What current benchmarks actually measure BFCL (Berkeley Function - Calling Leaderboard) BFCL is the de facto standard for function/tool - calling but focuses on call correctness, not answer faithfulness. Scope and data. ~2,000 question–function–answer pairs across Python, Java, JavaScript, REST, SQL; official counts include 100 Java, 50 JavaScript, 70 REST API, 100 SQL, and 1,680 Python cases. (All these counts are official - published.) Emphasizes serial vs parallel function calling, multi - function selection, and AST - based evaluation of the arguments sent to tools. Metrics. "Overall accuracy" is an unweighted average across subcategories; the core metric is whether the model constructs correct function calls (schema and arguments) for the test cases. (Definitions are official from the leaderboard/paper.) Newer versions (v3/v4) add categories like format sensitivity and multi - turn/agentic settings, but the scoring still centers on function - calling success, not checking whether the final natural - language answer faithfully restates tool outputs. (This reading of focus is third - party interpretation of their methods.) Leaderboards as of mid - July 2026. One public tracking site reports BFCL v4 evaluated at commit f7cf735, last updated 2026 - 04 - 12; it lists overall accuracy and cost/latency per model. (Dates and commit are official from the leaderboard.) Aggregators like LLM - Stats and PricePerToken list leaders (e.g., Llama 3.1 405B Instruct leading BFCL with ~0.885 score; GLM 4.5 at 76.7% on BFCL v3) and about 11–23 models evaluated. (These numbers are official for those leaderboards, but "defacto standard" is third - party characterization.) Gap vs your goal: BFCL does not inspect whether the agent's final message carries tool provenance, uncertainty, or refusal messages; it mostly stops at "did you call the function correctly and/or abstain when no function matches." τ - bench / τ³ - bench τ - bench evaluates tool - using conversational agents in dynamic, policy - constrained scenarios, with repeated trials to assess reliability. Scope. Original τ - bench targets airline and retail customer - service domains with APIs and policy rules; tasks require multi - turn conversations, database interactions, and adherence to policies. (Domains and high - level structure are official.) Tasks are defined as goal states in a database; evaluation checks whether the final database state matches the annotated goal. Metrics. Core metrics are domain - specific pass 𝑘 k (pass 1 1 –pass 4 4 ): probability of success within 𝑘 k trials; this is reliability over repeated runs, not faithfulness to specific tool outputs. (Definitions are official.) BenchLM's July 2026 τ - bench summary describes task success and reliability across repeated trials, but not fine - grained faithfulness of answers to intermediate tool responses. Gap vs your goal: τ - bench is about goal satisfaction under rules, not about whether the agent's narrative answer exactly reflects tool outputs or propagates tool refusals/provenance. There is no structured notion of "evidence envelope" fidelity in its metrics. ToolBench and ToolFailBench - style evaluations ToolBench and its successors focus on tool - use competence and planning, not envelope - level faithfulness. ToolBench. Built from 16,464 real - world REST APIs across 49 categories, with 126,486 instruction instances and 469,585 API calls; 2,746 evaluation tasks on a common leaderboard. (All counts are official.) Uses ToolEval, which computes Pass Rate (fraction of tasks successfully completed) and Win Rate (LLM - judge preference vs a reference), with ChatGPT - as - judge and ~87.1% human agreement on pass rate. (Numbers are official; reliance on LLM judges is explicit.) Emphasis is on whether the sequence of API calls solves the instruction; there is no explicit metric for whether the final textual answer carries tool - returned uncertainty or provenance. (This is third - party interpretation of their evaluation dimensions.) ToolFailBench (2026). A newer paper dedicated to diagnosing failure modes in tool - using agents; it categorizes errors like wrong tool selection, wrong parameters, and execution failures. (Scope and purpose are official from the abstract.) Evaluates failure rates across many models and tools, but again focuses on correctness of tool use and task success, not on faithful reproduction of tool outputs or structured refusals in agent answers. (This is third - party interpretation.) Gap vs your goal: these benchmarks care about "does the agent solve the task using tools," not "does the agent honestly transmit what tools said, especially when tools refuse or are uncertain." AgentBench, WildToolBench, and general agent evals AgentBench - style suites and newer tool - use evals like WildToolBench measure agentic task success across multiple domains. AgentBench family (2023–2025). Tests agents on web navigation, games, API use, and other tasks, with metrics like success rate and step efficiency; evaluation is usually programmatic (e.g., environment state) or via LLM - as - judge for open - ended tasks. (This is an aggregate description from benchmark directories; exact metrics vary by sub - benchmark.) Little to no explicit focus on whether the final answer accurately cites or propagates individual tool outputs. (Third - party interpretation.) WildToolBench (ICLR 2026). Evaluates 57 LLMs on 1,024 tasks drawn from real user behavior; reports that no model exceeds 15% session accuracy, emphasizing complex, compositional orchestration, hidden user intent, and instruction transitions as failure modes. (All these numbers and categories are official.) Metrics are session - level success, not envelope - level faithfulness; they track "did the session achieve the user's goal via tools," not whether intermediate tool outputs were correctly represented. (Third - party interpretation.) Again, these are about can agents do things, not "do they faithfully quote the instruments." Refusal handling and negative results The specific question "when a tool returns 'not found' / error / explicit refusal, does the agent fabricate anyway?" is barely covered explicitly in current benchmarks. BFCL "function relevance detection." BFCL includes a "function relevance detection" category, where the model must decide that no function is appropriate and return an error message instead of a tool call. (Category and goal are official.) This checks whether the model abstains rather than calling the wrong tool, but it does not evaluate post - hoc fabrication after a tool refuses; tools themselves are not returning structured "refusal envelopes" that must be propagated. (Third - party interpretation.) ToolEmu - style safety evals. ToolEmu and similar safety - oriented tool - use evaluations (early - mid 2020s) examine whether LLMs misuse tools for harmful purposes or circumvent safeguards, but their primary outcomes are safety violations / harmful actions, not faithfulness to explicit error or refusal messages from tools. (This is a generalized description; specific ToolEmu numbers are not in the retrieved snippets.) ToolFailBench. Focuses on diagnosing why tool - use fails (e.g., missing calls, wrong parameters, inability to handle tool errors), but not explicitly on whether the agent fabricates after error responses. (Scope is official; absence of refusal - faithfulness metrics is third - party interpretation.) No structured - refusal benchmarks. Across the benchmarks and overviews surveyed (BFCL, τ - bench, ToolBench, WildToolBench, MCP - Bench), none define a metric like "refusal - faithfulness rate: fraction of final answers that accurately represent a tool refusal when one occurs, without fabricating content." (This gap statement is third - party synthesis.) Your "typed not_found / credential_required / data_source_error refuse envelopes" plus deterministic grading against them is, as of mid - 2026, novel in the benchmarking landscape. Provenance, citation, and uncertainty fidelity There is related work in RAG citation faithfulness, but it almost never involves live tools or MCP - style envelopes. Citation - faithfulness benchmarks (e.g., VERICITE). VERICITE evaluates sentence - level citation faithfulness: for each sentence in a generated answer, does its cited source actually support the content? Metrics: per - sentence precision/recall for correct citations, hallucination rates, and disagreement between model explanations and sources; evaluation uses annotation plus automatic tools. (These definitions are official.) RAG attribution surveys. General RAG evaluation surveys compile techniques for measuring whether answers align with retrieved passages, using both automated entailment and LLM - as - judge. (Scope is official; details of metrics vary by included paper.) MCP - Bench mention of "evidence - based reasoning with citations." MCP - Bench's description lists "evidence - based reasoning with citations of tool outputs" as one of its stressors: agents are expected to ground answers in tool results and sometimes to provide references. (Wording and presence of this dimension are official.) However, public descriptions emphasize task completion metrics, planning quality, and tool orchestration; there is no clear, publicly documented metric that scores the fidelity of provenance/uncertainty propagation or structured envelopes similar to your design. (This is a third - party reading based on available summaries.) None of these works take envelopes that already separate value, confidence, assumptions, and provenance, and then grade whether agents transmit each field correctly. That is precisely where your design innovates: you can grade field - by - field fidelity deterministically. MCP - ecosystem - specific evaluation work MCP is now central to tool - use agent ecosystems, so there is some benchmark work specifically on MCP servers. MCP - Bench (Accenture). Connects agents to 28 production - grade MCP servers exposing 250 tools, spanning finance, research, health, travel, etc.; tasks are synthesized from tool I/O signatures and fuzzed to hide tool names. (Counts are official.) Task set: 104 tasks (56 single - server, 30 two - server, 18 three - server) in one public summary, with up to ~20 rounds allowed for complex workflows. (These numbers are official for that description.) Evaluation: dual; rule - based metrics (schema correctness, runtime success, dependency compliance) plus a rubric - driven LLM judge with prompt shuffling ×5 and averaged scores for Task Completion, Tool Usage, and Planning Effectiveness. (Judge structure and shuffle count are official.) It stresses "information grounding" and "evidence - grounded answers," but the public methodology does not expose a metric specifically checking tool - output vs final answer consistency at the level of individual values and cited sources. (Gap is third - party interpretation.) MCPToolBench++ and MCPAgentBench. MCPToolBench++ builds a large tool - use benchmark using a marketplace of over 4,000 MCP servers in 40+ categories, combining single - step and multi - step tool calls. (Counts are official.) MCPAgentBench focuses on tool - use abilities with candidate tool lists that include distractors, testing tool selection and discrimination; metrics revolve around task completion and execution efficiency. (Scope and metrics are official.) None of these MCP - focused benchmarks (a) plug into a scientific domain server with repo - pinned truth, (b) treat structured refusals as first - class evaluation targets, or (c) explicitly grade provenance/citation fidelity from tool outputs into the final answer. Lab - published agentic evals touching tool - output faithfulness Labs (OpenAI, Anthropic, Google, etc.) publish evaluation suites for their agents, but they mostly track task success, safety, and policy adherence, not envelope - level honesty. Anthropic — Claude Code and autonomy. Anthropic's 2026 report on agent autonomy in Claude Code analyzes millions of sessions, focusing on session length, auto - approval rates, intervention rates, and success on difficult tasks; they measure autonomy growth and risk management. (All percentages and time figures in that report are official.) While they highlight the importance of agents recognizing uncertainty and asking for help, they do not present a benchmark that quantitatively scores how faithfully Claude transmits tool outputs or refuses when tools fail. (Gap is third - party interpretation.) OpenAI / Google / Vertex AI agent benchmarks. News and governance summaries describe vendor benchmarks for code agents, web - navigation agents, and enterprise - workflow agents; metrics reported typically include success rate, latency, cost, and sometimes safety or rule - compliance measures. (These metrics are official.) Public documentation emphasizes high - level outcomes (tasks completed) and safety incidents rather than fine - grained comparisons between tool outputs and final answers. (Third - party synthesis.) No major lab has, as of mid - 2026, published a detailed, domain - specific, MCP - based honesty benchmark with deterministic grading of envelope - faithfulness similar to what you propose. Closest existing efforts and what they leave out Here are the 3–4 closest efforts to your intended study, with details so you can position your work precisely. 1. BFCL (Berkeley Function - Calling Leaderboard) Metrics and definitions. Overall accuracy: fraction of test cases where the model produces correct function calls (including schemas and arguments) for various languages and function setups. (Official definition.) Sub - metrics: accuracy by domain (Python, Java, JavaScript, REST, SQL), multiple vs parallel functions, and function relevance detection cases where no function should be called. Scenario count and coverage. ~2,000 question–function–answer pairs total; specific breakdown: 1,680 Python, 100 Java, 50 JavaScript, 70 REST API, and 100 SQL functions. (Official numbers.) Evaluated models: dozens across versions; one tracker lists 11–23 models depending on BFCL version. (Official counts for those leaderboards.) Grading method. Programmatic AST - based evaluation of function calls against expected behavior; some multi - step agentic evaluation (v4) also uses programmatic checks and heuristics. (Official.) What it does not measure. It does not check whether the final textual answer contains accurate echoes of tool outputs, includes or preserves provenance fields, or respects structured refusals beyond abstaining from calling any function. (Gap is third - party interpretation.) 2. τ - bench / τ³ - bench Metrics and definitions. pass 𝑘 k (k=1–4) task success and reliability over repeated trials in airline/retail customer service tasks using domain tools and policies. (Official definitions.) Emphasis is on whether the agent drives the environment to the goal state while following constraints. Scenario count. The original 2024 τ - bench release provides archived airline and retail task files; current benchmark directories note that these are now "outdated" and direct users to a maintained τ³ - bench repository. (The existence and status of these sets are official; exact counts per domain are in underlying repos but not fully visible from the snippet.) Grading method. Programmatic comparison between the end database state and the annotated goal state. (Official.) What it does not measure. Fine - grained fidelity of answer text to individual tool outputs, or explicit propagation of tool uncertainty, assumptions, or provenance. (Third - party interpretation.) 3. ToolBench Metrics and definitions. Pass Rate: proportion of instructions successfully completed according to ground - truth traces. Win Rate: LLM - judge preference vs a reference model (ChatGPT) for outputs, with 87.1% human agreement on pass rate and 80.3% on win rate. (All values are official.) Scenario count and coverage. 16,464 APIs across 49 categories, 126,486 instruction instances, 469,585 API calls, 4.0 reasoning traces per instance on average; evaluation uses 2,746 tasks. (All counts are official.) Models include GPT - 4 (with DFSDT), ToolLLaMA, and others; GPT - 4 with DFSDT achieves 71.1% pass rate and 70.4% win rate. (Official.) Grading method. Programmatic evaluation of whether traces satisfy task goals plus LLM - as - judge for win - rate comparisons. (Official.) What it does not measure. It does not inspect whether agents faithfully reproduce a tool's stated conditions or provenance in their final natural - language answer, nor track behavior conditioned on explicit tool refusals. (Third - party interpretation.) 4. MCP - Bench Metrics and definitions. Task Completion, Tool Usage, Planning Effectiveness, and evidence - grounded answers scored via a combination of rule - based execution checks and LLM judging with a rubric. (Names and approach are official.) Stressors include distractor servers, fuzzy instructions, and long - horizon workflows up to ~20 rounds. (Numbers and descriptions are official.) Scenario count and coverage. 104 benchmark tasks: 56 single - server, 30 two - server, 18 three - server scenarios. (Official.) 28 MCP servers and 250 tools. (Official.) Grading method. Rule - based metrics over tool calls (e.g., schema correctness, runtime success) plus an LLM - judge with prompt shuffling ×5 and score averaging for more qualitative dimensions. (Numbers and approach are official.) What it does not measure. Any publicly described metric that checks field - level faithfulness between tool outputs and final answers, or deterministic grading against structured evidence envelopes. (Gap is third - party interpretation.) The gap your astrodynamics - MCP honesty study fills From the above, the missing pieces your study would uniquely provide as of July 2026 are: Domain - grounded scientific tools with repo - verified truth. Existing benchmarks mostly target generic APIs (ToolBench), enterprise tasks (τ - bench), or heterogeneous MCP servers (MCP - Bench) where underlying ground - truth science is not central. Your astrodynamics MCP server is explicitly tied to validated orbital mechanics algorithms and upstream catalogs, documented in a public repo. (Domain characterization is official; "uniqueness" is third - party synthesis.) Evidence envelopes and structured refusals as first - class evaluation targets. No prior benchmark defines typed refusals (e.g., not_found, credential_required, data_source_error) as objects of study or grades whether agents faithfully surface them vs fabricating. Your design explicitly does this. (Novelty statement is third - party synthesis from comparing benchmark specs.) Provenance/assumptions/confidence propagation metrics. Citation - faithfulness work in RAG (VERICITE) exists, but is not integrated with live tool calls or MCP; none of the tool - use benchmarks track whether agents preserve the tools' own stated confidence, assumptions, or provenance fields. Deterministic grading against structured envelopes and repo - verified truth. MCP - Bench mixes rule - based checks with LLM judges; ToolBench uses LLM judges heavily; τ - bench and BFCL are programmatic but don't evaluate envelope - faithfulness. With structured JSON envelopes and astrodynamics ground truth, you can deterministically grade: Value fidelity (numerical equality / tolerance). Refusal - faithfulness (did the final answer accurately represent the refusal code and what_would_help, without hallucinated values). Provenance/assumptions/uncertainty propagation (field - by - field). Explicit focus on over - rides by parametric memory vs tools. Benchmarks rarely isolate "tool vs pretrained prior conflict" cases; your scenarios can be designed to create known conflicts (e.g., outdated ephemeris vs current tool catalogs) and grade which one the agent follows. This gives you a crisp positioning statement: "Existing benchmarks measure tool - call correctness and task success; none directly measure how faithfully LLM agents transmit structured tool evidence, especially refusals and provenance. This study fills that gap in a high - stakes scientific domain with deterministic grading." LEVEL 1: Three decision - relevant follow - up questions 1. Which existing benchmark family should you anchor to as the primary comparison point when justifying your metrics? Why it matters: Reviewers will ask "why isn't this just a small variant of BFCL / MCP - Bench / ToolBench?"; anchoring to the closest precedent clarifies your contribution. The closest precedents by evaluation dimension are BFCL (function/tool correctness), ToolBench (large - scale tool use with LLM judges), τ - bench (agent reliability across trials), and MCP - Bench (MCP - specific, evidence - grounded tool use). BFCL anchors function - call correctness with programmatic grading and some abstention cases, but no final - answer faithfulness. MCP - Bench anchors MCP - based, evidence - grounded tasks across many servers, mixed rule - based + LLM - judge grading. ToolBench and τ - bench anchor task - completion and reliability metrics (pass rate, win rate, pass 𝑘 k ) and multi - trial evaluation. Given your interest in envelope - faithfulness on MCP tools, the best anchor is: Primary: MCP - Bench (MCP ecosystem, evidence - grounded tasks). Secondary: BFCL for function correctness framing, τ - bench for pass 𝑘 k reliability, and VERICITE for citation - faithfulness analogies. This lets you say: "We follow MCP - Bench in using real MCP servers and programmatic grading of tool traces, but extend it with BFCL - style correctness checks and VERICITE - style faithfulness metrics applied to structured evidence envelopes." (Benchmark selection is third - party design guidance; benchmark properties are official.) 2. Should your primary outcome be scenario - level envelope - faithfulness (per - scenario rate) or per - run task success (like pass 𝑘 k / pass rate)? Why it matters: The primary outcome defines how you summarize results and where you align with / diverge from existing benchmarks. ToolBench and τ - bench use task - level success over runs (pass rate, pass 𝑘 k ), while BFCL uses per - example correctness of function calls; none treat per - scenario envelope - faithfulness as a first - class metric. MCP - Bench mixes multiple outcome dimensions (task completion, planning, tool usage). Given your research question ("do agents faithfully transmit evidence?"), the natural primary metric is: Scenario - level envelope - faithfulness rate per model: fraction of scenarios (aggregated over r repetitions) where the final answer satisfies all envelope - faithfulness constraints (value, refusal, provenance/assumptions fields). (This definition is third - party - proposed but structurally mirrors existing per - task accuracy metrics.) You can then: Report per - model faithfulness rates with CIs as primary, akin to BFCL's overall accuracy but defined at the envelope level. Include task success (e.g., "did the user question get correctly answered?") as a secondary outcome, aligning with ToolBench and τ - bench. This design makes your study clearly about honesty/faithfulness, not just competence, while staying compatible with familiar forms of summary statistics and leaderboards. 3. How many distinct envelope - faithfulness dimensions should you grade separately (value, refusal, provenance, uncertainty, assumptions) vs collapsing into a single "faithful / not" label? Why it matters: Each extra dimension multiplies design and reporting complexity, but collapsing everything risks losing the specificity that differentiates your work. Existing benchmarks generally expose 2–4 core metrics: ToolBench (pass rate, win rate), BFCL (overall accuracy + a few sub - category accuracies), τ - bench (pass 𝑘 k ) and MCP - Bench (task completion, tool usage, planning, sometimes evidence - grounding). Citation - faithfulness work like VERICITE evaluates faithfulness at the sentence level but still condenses it into a small set of metrics (e.g., precision/recall). By analogy, and given your structured envelopes, a defensible set is: At least 3 primary faithfulness dimensions: Value fidelity: are numeric outputs consistent with envelopes within tolerances? (Domain references are official; metric design is third - party.) Refusal fidelity: when tools return refusals, does the agent correctly represent that refusal without fabricating alternative values? Provenance/assumptions/uncertainty fidelity: does the answer preserve the envelope's source, assumptions, and confidence information (e.g., citing the correct catalog / commit and not overstating certainty)? Optionally, you can aggregate these into a single "strict faithfulness" indicator ("all fields respected") for headline comparison, but keep the components visible in tables/appendix, mirroring how MCP - Bench and ToolBench report sub - metrics alongside an overall score. This strikes the right balance between granularity (what's interesting about envelopes) and interpretability (a small metric set). 

LEVEL 2: Follow - ups for answers affecting design 1A. Given MCP - Bench as the primary anchor, should you also incorporate repeated - trial reliability metrics like τ - bench's pass 𝑘 k to discuss robustness of honesty? Why it matters: Faithfulness can be brittle; repeated - trial metrics capture whether honesty is consistent across runs, echoing tau - style reliability. τ - bench explicitly introduces pass 𝑘 k to quantify reliability of agent behavior over multiple trials, showing that even strong agents have low pass 8 8 in some domains. MCP - Bench allows up to ~20 rounds per task but focuses on a single run per configuration in published summaries, not pass 𝑘 k . For an honesty study, it is compelling to ask: how often does a model stay honest over repeated episodes? A direct adaptation is: Define faithfulness - pass 𝑘 k : for each scenario, run the agent r times; faithfulness - pass 𝑘 k is the fraction of scenarios where at least one of k runs is fully faithful (upper - bound reliability) or where all of k runs are faithful (strict reliability). (This metric is third - party - proposed, but structurally mirrors τ - bench's pass 𝑘 k .) You can then: Keep MCP - style single - run metrics (per - run and per - scenario faithfulness) for comparison to MCP - Bench. Add faithfulness - pass 𝑘 k (e.g., k=3 or 5) as a secondary robustness metric, justified by analogy to τ - bench's reliability analysis. This helps you show not only that a model can be honest sometimes, but whether it is reliably honest under repeated sampling. 2A. For scenario - level envelope - faithfulness, how should you define "success" per scenario when there are multiple envelope dimensions and repeated runs? Why it matters: The precise per - scenario success definition drives your main numbers and affects how models compare. Existing benchmarks with multiple dimensions usually define a task as "passed" only if all necessary conditions are met: ToolBench's pass rate requires that the entire API sequence satisfies the goal; BFCL's correctness demands fully correct calls; τ - bench's pass 𝑘 k requires reaching the goal state. By analogy, a conservative and methodologically legible definition is: For each run, an episode is faithful only if all targeted envelope dimensions are satisfied (value, refusal, provenance/assumptions/uncertainty as applicable). (Structure is third - party - designed but analogous to existing definitions.) For each scenario, define per - model success rate as the fraction of runs that are fully faithful; a scenario counts as "passed" in a strict sense if all r runs are faithful, or you can report both "mean run - level faithfulness" and "strict scenario - level pass rate" (0/1 per scenario). This yields per - model metrics compatible with BFCL - style accuracy (mean run - level faithfulness) and τ - style reliability (scenario - level strict pass rates, plus faithfulness - pass 𝑘 k ). 3A. How many envelope - faithfulness dimensions can you realistically grade deterministically without drifting into LLM - judge territory? Why it matters: Deterministic grading is a key selling point; you want to avoid slipping back into LLM - judged semantics. ToolBench uses LLM judges for complex reasoning traces; MCP - Bench uses LLM judges for planning quality and some evidence - grounding aspects; VERICITE uses human or LLM judgments to assess sentence - level citation faithfulness. These experiences suggest that grading free - form content beyond structural properties tends to require judges. In your case, because you control the envelope schema, you can design grading to remain structural: Value fidelity: programmatically compare numeric outputs in the answer against envelope values (with domain - specific tolerances informed by astrodynamics references, e.g., expected digits of accuracy for Lambert solvers). (Expected precision is official for those solvers; using them as tolerance guidance is third - party design.) Refusal fidelity: pattern - match whether the answer explicitly states the refusal type and does not introduce numeric value fields that are absent from the envelope; you can design answer templates or required markers to make this deterministic. (Design is third - party, informed by your MCP server's error envelopes.) Provenance/assumptions/uncertainty fidelity: restrict these fields to enumerated strings or IDs (e.g., catalog = "Space - Track", "ESA DISCOSweb", commit hash ID, assumption labels); then check whether the answer includes the correct IDs/labels. (Official description of credential and provenance mechanisms; grading schema is third - party.) Anything beyond this (e.g., nuanced natural - language hedging or cross - sentence attribution) risks needing LLM judges. To keep the "strictly stronger than LLM - judge" value proposition, you should deliberately constrain the answer format (e.g., JSON or structured sections) so that all faithfulness dimensions are checkable programmatically. 

LEVEL 3: Follow - up for remaining decision weight 3A - i. Should you enforce a structured answer format (e.g., JSON sections for value/refusal/provenance) in prompts to guarantee deterministic grading? Why it matters: Without structured answers, deterministic envelope - faithfulness grading may be brittle; structured outputs trade some ecological validity for grading reliability. BFCL and many tool - use benchmarks already enforce structured outputs—function call JSON or code snippets—because they are easier to evaluate automatically. MCP - Bench tasks use structured tool schemas and often require agents to return JSON or semi - structured outputs for consistent scoring. VERICITE's sentence - level attribution also effectively depends on sentence segmentation and explicit citation markers. Given your goal of deterministic, envelope - aware grading, adopting a semi - structured answer schema is a pragmatic choice: Require agents to respond with, for example, a top - level JSON object with fields user_answer (free - form explanation), tool_value_summary, refusal_status, provenance, assumptions, confidence, each with constrained formats that map directly to envelope fields. (This exact schema is third - party - proposed but matches common practice in function - calling APIs and BFCL - style evaluations.) Prompts can still instruct agents to write user - facing natural language inside user_answer, preserving ecological realism, while the structured fields support exact grading. This approach aligns your harness with field norms around structured evaluation outputs and maximizes the credibility of your claim that you have deterministic, judge - free honesty metrics. LOAD - BEARING NUMBERS Each line is a number that could influence design, with source and type (official vs third - party - estimated/design): ~2,000 question–function–answer pairs in BFCL benchmark; includes 1,680 Python, 100 Java, 50 JavaScript, 70 REST API, 100 SQL. (Official.) 28 MCP servers and 250 tools in MCP - Bench. (Official.) 104 tasks in MCP - Bench: 56 single - server, 30 two - server, 18 three - server. (Official.) Up to ~20 rounds allowed per MCP - Bench multi - server task. (Official.) 16,464 APIs, 49 categories, 126,486 instruction instances, 469,585 API calls, 4.0 reasoning traces per instance, and 2,746 evaluation tasks in ToolBench. (Official.) 71.1% pass rate and 70.4% win rate for GPT - 4 with DFSDT on ToolBench. (Official.) 87.1% human agreement on pass rate and 80.3% on win rate for ToolBench's LLM - judge evaluation. (Official.) 57 LLMs and 1,024 tasks in WildToolBench; no model exceeds 15% session accuracy. (Official.) τ - bench pass 𝑘 k metrics (pass 1 1 –pass 4 4 ) used to measure reliability across repeated trials. (k values and concept are official.) Faithfulness - pass 𝑘 k metric (e.g., k=3 or 5) proposed for your study as an adaptation of τ - bench; k choice is third - party - estimated. 4,000+ MCP servers across 40+ categories used in MCPToolBench++ datasets. (Official.) 3–4 core metrics common in benchmarks (e.g., ToolBench's pass rate + win rate; MCP - Bench's Task Completion, Tool Usage, Planning, + optional evidence grounding). (Metric counts are official; "3–4" as typical is third - party - summarized.) 3 envelope - faithfulness dimensions recommended for separate grading: value fidelity, refusal fidelity, provenance/assumptions/uncertainty fidelity. (Number is third - party design choice.) 30 scenarios in your planned study (design parameter; not from literature). r (repetitions per scenario per model) will interact with faithfulness - pass 𝑘 k metrics; while not fixed here, the notion that k might be 3 or 5 for reliability analysis is third - party - estimated based on τ - bench. If you'd like, next steps could be to co - design the exact JSON schema for your evidence envelopes and the structured answer format so that all of these metrics are programmatically computable. LLM-agent evaluation work through mid - 2026 is strong on "did the model call the right tools and succeed at the task?" but very thin on "did the model's final answer faithfully transmit what the tools actually returned, including refusals, provenance, and explicit uncertainty." Your proposed astrodynamics MCP study would sit almost directly in that gap: domain - grounded scientific tools, typed refusals as first - class outputs, repo - verified numeric ground truth, and explicit grading of provenance/assumption transmission. Below I walk through the current state, then the follow - up chain you requested. 1. Tool - use faithfulness vs tool - call correctness Most mainstream "tool use" benchmarks evaluate whether the model selects and parameterizes tools correctly and completes tasks, not whether the final natural - language answer accurately mirrors the tool outputs. BFCL (Berkeley Function Calling Leaderboard, 2025–2026) focuses on whether the model issues correct function calls (API signatures, arguments) across BFCL - Fundamental, BFCL - Live, and BFCL - Agent datasets, using AST - based matching and some execution as validation. It reports overall accuracy, cost, and latency; the core correctness signal is correspondence between expected and produced function calls (plus some "abstain when function irrelevant" tests), not the fidelity of any subsequent explanation or summary. ToolBench (Qin et al. 2023, ToolLLaMA) evaluates multi - API planning and calling on RapidAPI - derived tasks; metrics are pass rate and win rate judged by ToolEval (LLM - as - judge). What matters is whether the task is solved given API traces; it does not separately score whether the final textual answer faithfully reflects all tool responses, let alone preserves uncertainties or failure modes. AgentBench (Liu et al., ICLR 2024) and AgentBench - FC measure task success across 8 environments (OS, DB, KG, games, ALFWorld, web shopping/browsing, puzzles) with success defined in environment - specific terms (e.g., script output correctness, query result accuracy, task completion). Even the function - calling extension is about action correctness and success rates, not about whether an agent misreports or fabricates around tool outputs in its final messages. τ - bench / τ² (tool - agent - user benchmark) evaluates agents in airline and retail domains under human - like constraints; metrics are pass^k (probability of solving a task within k independent runs) across domains. Again, what matters is whether the agent accomplishes the user goal (e.g., booked correctly, policy respected), not whether its final prose matches database responses one - for - one. Lab "agentic" evals (OpenAI, Anthropic, Google DeepMind) typically report success rates on internal tool - calling suites, web tasks, shopping, code execution, or document QA, plus qualitative error analyses. Public descriptions emphasize reliability and safety at the task level; they rarely describe a metric that checks whether the agent preserves tool provenance and uncertainty vs. rewriting or ignoring it in the final answer. So, the dominant paradigm is: "tool correctness + task success," not "tool - to - answer faithfulness." Your study is explicitly about the latter. 2. Benchmarks and papers closest to tool - output faithfulness 2.1 τ - bench / τ² τ - bench is a benchmark for tool - agent - user interaction in real - world airline and retail workflows. It defines: Tasks & scenarios: Dynamic, multi - turn scenarios in airline and retail domains where the agent interacts with tool APIs under realistic constraints. Metrics: Pass^k – fraction of tasks successfully completed in up to k independent trajectories (pass^1, pass^2, etc.). As of 2024 - 10 - 22, for the retail domain, claude - 3.5 - sonnet - 20241022 achieves pass^1 0.692, pass^2 0.576, pass^3 0.509, pass^4 0.462 (official numbers on the τ - bench site). These are official - published. Grading: Programmatic checks combined with human or scripted policy constraints; trajectories are evaluated offline based on tool calls and environment state transitions. Crucially, τ - bench does not separately score: Whether the final answer explicitly cites the tools or passes along their uncertainties. Whether the agent avoids fabrication when APIs lack needed information. The EMNLP 2025 "IRMA" paper on τ - bench analyzes error types (reasoning inconsistencies, failures to apply rules) and proposes input reformulation to improve pass^5, but again evaluates via pass^k, not a distinct "truthfulness to tools" metric. 2.2 BFCL v4 BFCL v4 (Patil et al., ICML 2025) is the most thorough function - calling benchmark as of mid - 2026. Structure: BFCL - Fundamental (single/parallel calls), BFCL - Live (67k+ community examples), BFCL - Agent (1k multi - API scenarios). Metrics: Abstract Syntax Tree (AST) substring matching between predicted and reference function calls as a proxy for execution - based correctness. On subsets, executed functions show strong correlation between AST - based and execution - based metrics, justifying AST as the main scalable metric. Leaderboard reports overall accuracy (unweighted average over subcategories), plus cost and latency.

Scenario counts: For the original BFCL, ~2k question - function - answer pairs with language/API breakdown 100 Java, 50 JavaScript, 70 REST API, 100 SQL, and 1,680 Python across scenarios including function - relevance detection. BFCL includes "function relevance detection", where models must correctly abstain when the provided function is not suitable; in that case "an error message will be provided." That's the closest analogue to testing abstention/"no suitable tool" behavior. But: BFCL evaluates whether the model calls a function at all (or correctly abstains) and whether the call is correct; it does not track what the model says in natural language after the call. There is no per - example notion of "tool output contained X; did the explanation faithfully state X with correct provenance." So BFCL is very close in spirit for abstention and call integrity, but it lacks the tool - to - answer faithfulness dimension your study targets. 2.3 ToolBench (ToolLLaMA / ToolEval) ToolBench's ToolEval is designed for automatic evaluation of tool - augmented traces. Tasks & scale: 16,464 APIs, 49 categories, 126,486 instruction instances, 469,585 API calls (official - published). Metrics: Pass Rate: fraction of instructions successfully completed. Win Rate: preference against reference model using an LLM judge (ChatGPT), with human agreement of 87.1% on pass rate and 80.3% on win rate (official - published).

Grading method: LLM - as - judge, comparing full trajectories and final answers to a reference; heavily qualitative. ToolEval inspects both tool traces and answers, but its target is "was the user instruction fulfilled?" not "did the answer faithfully express what the tools returned, with uncertainties and explicit refusals preserved." Also, the tool output semantics are heterogeneous REST APIs, not a tightly typed scientific domain. 2.4 AgentBench and other agentic suites AgentBench evaluates agent success across environments, with metrics like success rate, composite scores, and environment - specific definitions. Scale: ~1,091 tasks across 8 environments in the core AgentBench, according to independent benchmark documentation (this specific number is third - party - estimated from benchmark aggregators, not the original paper). GPT - 4's composite score of 4.27 vs. Llama - 2 - 13B - chat ~1.01 is also from an independent summary and labeled as approximate (~), so treat it as third - party - estimated. Metrics: Task success rate; composite scores. These environments involve tool - like interaction (e.g., bash, SQL, web browsing), but there is no explicit notion of "did the agent faithfully represent the tool results in its narrative answer." Other agentic benchmarks (ML - TOOL - BENCH, web agents, etc.) likewise score task completion and sometimes cost/latency. None of the primary sources describe a dedicated metric for tool - output faithfulness in the final answer. 3. Refusal and negative - result handling Your question about negative results and refusals—"not found," explicit errors, structured refusals—is one of the clearest gaps. BFCL's "function relevance detection" is the closest: when the correct behavior is to avoid calling any function because none is relevant, the model is rewarded for abstaining. However, this is about the choice not to call a function; there is no notion of tools returning a structured refusal (e.g., not_found) that must be faithfully reported. τ - bench includes policy constraints and failure modes (e.g., cannot find a fare that meets constraints), but evaluates whether the agent ultimately follows domain rules and accomplishes the task; it does not, as of the published descriptions, score whether an agent fabricates a booking or policy exception after receiving a failure from the tool APIs. ToolEmu (Identifying the Risks of LM Agents with an LM - Emulated Sandbox) introduces a tool - emulated environment and LLM - based evaluator to study agent risks and failure modes (e.g., security, safety). They report that 68.8% of failures identified with ToolEmu would be valid based on human evaluation, which is an official - published figure from the paper. But this work focuses on risk behaviors (e.g., harmful tool use, policy violations), not on faithful propagation of tool errors/refusals into spoken answers. I could not find any benchmark as of mid - 2026 that explicitly provides typed tool refusals (e.g., not_found, invalid_parameters, insufficient_data) with associated "what would help" and then checks whether the agent (a) refuses, (b) fabricates, or (c) distorts the refusal in the final answer. So handling of explicit tool - level refusals as first - class results is essentially unmeasured in public benchmarks. Your "evidence envelope" design and evaluation focus land squarely here. 4. Provenance/citation and uncertainty fidelity There is a related stream of work in RAG and attribution, but it usually deals with document grounding, not function/tool outputs. Representative threads: Attribution/faithfulness in RAG: A range of work (e.g., RFEVAL: "Benchmarking Reasoning Faithful…", and other attribution benchmarks) evaluates whether an answer's supporting evidence is actually present in retrieved documents and whether citations are grounded. These often: Provide documents as input. Ask models to answer with citations. Score whether cited spans support the answer (sometimes with programmatic spans, sometimes LLM - as - judge). Agentic RAG faithfulness: Some applied work on agentic RAG systems evaluates whether agents preserve citing, avoid hallucinating sources, or misattribute spans (e.g., in e - governance or enterprise settings). These tend to be domain - specific and framed around document support, not structured function returns. What is missing relative to your design: Tools as structured evidence: I could not locate, in primary benchmark sources, a standardized setting where tool outputs are treated as structured evidence with: explicit provenance (e.g., Git commit or artifact ID), explicit uncertainty/confidence, explicit assumptions, validity intervals, and agents are scored on whether they preserve and present this correctly. Citation fidelity for tools: Existing attribution metrics generally treat citations as text spans (URLs, document IDs), not "this tool call produced this number at commit X under assumptions Y." So there is adjacent work on textual grounding and citation, but essentially no tool - output provenance fidelity benchmark. 5. MCP - ecosystem evaluation as of mid - 2026 As of mid - 2026, public MCP ecosystem resources focus on specification, SDKs, and server catalogs, not systematic evaluation of agent faithfulness to MCP tool outputs. The MCP specification and SDKs (e.g., Python SDK v2 pre - releases, TypeScript SDK on npm) describe protocol semantics and provide dev tooling; they do not include formal evaluation suites or benchmarks. MCP "marketplaces" or registries list servers for domains like astrodynamics, but they are discovery/catalog systems (e.g., PulseMCP, Claude Code marketplaces). They do not publish systematic metrics on how agents behave when interacting with those servers. I could not find any published MCP - specific benchmark/harness whose primary target is "faithful propagation of MCP tool outputs into agent answers." Some experimental projects (e.g., MCPverse references in agent testing papers) mention MCP servers as environments, but details are sparse and focus on task success rather than evidence fidelity. Your aster - mission - mcp server, with evidence envelopes and structured refusals, is therefore not just a tool but potentially the first domain - grounded MCP testbed designed explicitly for honesty/faithfulness measurement. 6. Lab - published agentic evals touching tool - output faithfulness Publicly documented lab evals (OpenAI, Anthropic, Google DeepMind, others) are mostly task - level: Anthropic (e.g., Claude 3, Claude 3.5 Sonnet) reports performance on τ - bench airline/retail, SWE - bench, WebArena, RAG QA, etc., with metrics like success rate and pass^k. They emphasize reliability and adherence to tools and policies, but public material does not give a formal metric like "fraction of answers where the numbers and uncertainties match tool outputs exactly." OpenAI similarly reports tool use success on internal or public benchmarks (e.g., via BFCL, web agents, coding tasks), focusing on solved tasks, latency, and cost. Tool - output faithfulness is discussed qualitatively ("grounded," "use tools when needed") rather than with explicit quantitative metrics. Google DeepMind and others (e.g., in recent agent benchmark comparisons) emphasize success rates in interactive environments (navigation, shopping, code editing, etc.), again without a specific metric for whether explanations faithfully mirror tools. Where labs get closest is in safety and risk evals like ToolEmu and internal red - team suites, which look for misuses and hallucinations, but those are framed as risk behaviors, not as a general "fidelity score" across a broad test set. So, as of mid - 2026, no major lab has publicly released a benchmark whose headline metric is "faithfulness of final natural - language answers to tool outputs," especially with typed refusals and provenance. 7. Where your study fits the gap Given the above, your proposed study would fill several concrete holes: From task success to evidence fidelity Existing benchmarks largely conflate "agent got the task right" with "agent used tools correctly," and they rarely inspect whether the final explanation faithfully reflects tool outputs. Your design explicitly measures: Numeric correctness vs. tool output. Whether the agent invents values contradicting tools. Whether uncertainty ranges and assumptions are preserved. Refusals and negative results as first - class Today, benchmarks occasionally test "abstain/do nothing" when no function is relevant (BFCL relevance detection), but they do not model tools that themselves return typed refusals with guidance. Your evidence envelopes make refusals a core data type, allowing metrics like "refusal - faithfulness rate" (fraction of refusal cases where the agent explicitly refuses without fabrication vs. hedging vs. hallucinating). Provenance and temporal validity Current work on provenance is mainly document - citation; tool - level provenance (commit hashes, artifact IDs, version dates) is unscored. Your setup allows deterministic checks that the agent: Mentions the correct source/commit or at least an unambiguous artifact ID. Does not misrepresent validity envelopes (e.g., extrapolating beyond validity dates). Domain - grounded scientific tools + deterministic grading Most tool benchmarks lean on consumer APIs (retail, travel, web), system tools (bash, SQL), or general APIs. Astrodynamics provides: Hard, numerically precise ground truth (Lambert solutions, porkchop scans, feasibility constraints). An easy path to deterministic replay at a frozen commit. This enables entirely programmatic, non - LLM grading for many dimensions (numeric equality/tolerance, citation correctness, refusal classification), in contrast to ToolBench's LLM - as - judge approach. MCP as a standardized substrate With MCP, the same evaluation harness can be reused across models and hosts with minimal glue, and the evidence envelope semantics are portable. This opens a path toward an MCP - native faithfulness benchmark, which does not currently exist. 

8. Closest existing efforts: detailed metrics, scale, and what they omit Here are 3–4 closest efforts, with the specifics you asked for. 

8.1 BFCL v4 (Berkeley Function Calling Leaderboard) Metrics (official): Overall Accuracy: unweighted average of sub - category accuracies (Fundamental, Live, Agent, etc.). Sub - metrics based on AST substring matching between model and reference function calls; validated via correlation with execution - based checks. Cost (USD estimate per benchmark run) and latency (seconds). Scenario counts & structure (official): BFCL includes 100 Java, 50 JavaScript, 70 REST API, 100 SQL, and 1,680 Python cases across function calling scenarios (simple, parallel, multi - function, relevance detection). BFCL - Live adds more than 67k real - life function - calling examples.

Grading method: Programmatic AST matching; limited execution - based evaluation for a subset. No LLM - judge in core metric.

Model coverage: Leaderboard lists multiple commercial and open - source models; as of 2026 - 04 - 12, results are tied to commit f7cf735; users can reproduce via bfcl-eval==2025.12.17 (all official - published details). What it does NOT measure: Faithfulness of the final natural - language answer to function outputs. Handling of structured tool refusals as first - class values. Provenance/citation transmission beyond "correct API and arguments." 8.2 τ - bench / τ² Metrics (official): Pass^k for k = 1…4 on the public leaderboard (airline and retail domains). For example, retail results for claude - 3.5 - sonnet - 20241022: pass^1 0.692, pass^2 0.576, pass^3 0.509, pass^4 0.462. These numbers are official - published on tau - bench.com. Scenario counts & structure: Two main domains (airline, retail), each with multiple tasks modeled as interactive user–agent–tool dialogs; precise total task count is described qualitatively in papers and blog posts but not always as a single headline number. Any single aggregate count from secondary sites would be third - party - estimated.

Grading method: Programmatic success/failure checks based on environment state and domain rules; pass^k is computed over independent runs.

Model coverage: Multiple commercial models and strategies (tool - calling TC vs others) with dated entries. What it does NOT measure: Fidelity of natural - language answers to tool outputs. Structured tool refusal handling (beyond policy constraints). Provenance or citation behavior. 

8.3 ToolBench + ToolEval Metrics (official): Pass Rate: fraction of instructions successfully completed. Win Rate: preference vs. reference model judged by ChatGPT; human agreement: 87.1% on pass rate, 80.3% on win rate (both official figures). Scenario counts & structure (official): 16,464 REST APIs, 49 categories, 126,486 instruction instances, 469,585 API calls; average 4.0 reasoning traces per instance.

Grading method: LLM - as - judge (ToolEval) comparing trajectories/answers against a reference; partial human validation.

Model coverage: GPT - 4 with DFSDT reaches 71.1% pass rate and 70.4% win rate; ToolLLaMA - 2 - 7B - v2 reaches 66.7% pass rate (all official - published). What it does NOT measure: Per - tool - call numeric faithfulness in final answers. Systematic handling of structured refusals or explicit uncertainties. Provenance/citation fidelity. 

8.4 ToolEmu (LM - Emulated Sandbox) Metrics (official): Focus on identifying risky behaviors; they report that 68.8% of failures identified with ToolEmu would be valid according to human evaluation (official number). Scenario counts & structure: Various simulated tool environments with attacker/victim dynamics; details are task - and tool - specific.

Grading method: LLM - based emulator plus human evaluation; emphasis on coverage of risk patterns rather than exact numeric correctness. What it does NOT measure: Routine evidence fidelity in normal usage. Structured refusals or provenance transmission. Compared to these, your study's key additions would be: Domain: tightly specified astrodynamics tools with numerical ground truth. Evidence semantics: explicit evidence envelopes (values/refusals + provenance + confidence + assumptions + validity). Metrics: multi - dimensional faithfulness (numeric equality/tolerance, refusal fidelity, provenance correctness, uncertainty/assumption preservation) separated from mere task success. Grading: fully deterministic programmatic grading tied to a pre - registered repository commit. 

LEVEL 1 FOLLOW - UPS Now I'll follow your requested chain: 3 Level - 1 follow - ups (each: why it matters, then answer with sources), then at most one Level - 2 off any that materially affect design, then Level - 3 if warranted, staying under 10 follow - up answers total. 

L1.1: Should you use programmatic grading only, or mix in LLM - as - judge? 

Why this matters: Your design choices here determine reliability, scalability, and comparability to existing benchmarks like ToolBench and ToolEmu. Answer (

L1.1): Programmatic grading is preferred whenever outcomes can be deterministically checked against ground truth, as BFCL and τ - bench demonstrate. BFCL uses AST substring matching and validates it against execution - based metrics, finding strong correlation, which supports using programmatic checks for large scale evaluation. τ - bench relies on programmatic pass^k checks over environment state with no LLM - judge in the main metric. LLM - as - judge is used when programmatic grading is hard—for example, ToolBench uses ChatGPT to judge pass/win rate against references, backing it with 87.1% and 80.3% human agreement. ToolEmu likewise relies on LLM and human evaluation to classify risky behaviors. These approaches introduce noise, bias, and cost but enable evaluation of fuzzy qualities (e.g., explanation quality). Given that your astrodynamics MCP server outputs precise numeric results, structured refusals, and explicit metadata, you can structure core metrics—numeric fidelity, refusal fidelity, provenance correctness—as purely programmatic, keeping LLM - as - judge only for residual qualitative aspects if needed. This aligns your study with BFCL/ τ - bench's reliability while still being comparable to ToolBench/ToolEmu for higher - level behaviors. Design implication: Core honesty metrics should be exact, code - checked; if you want a "helpfulness" layer, consider LLM - judge as an auxiliary, not primary, metric. 

L1.2: How many scenarios and dimensions do you need for the study to be meaningfully distinct from τ - bench/BFCL/ToolBench? 

Why this matters: Scenario count and coverage determine statistical power and how credibly you can claim incremental contribution. Answer (

L1.2): Existing benchmarks span from hundreds to tens of thousands of tasks: BFCL's core function categories total roughly 2,000+ cases (100 Java, 50 JS, 70 REST, 100 SQL, 1,680 Python) plus 67k+ BFCL - Live examples. ToolBench has 126,486 instruction instances over 16,464 APIs—very large scale. AgentBench's core set is ~1,091 tasks across 8 environments (this count is third - party - estimated). τ - bench has a smaller but still nontrivial number of airline and retail tasks; counts are not always given as a single headline number but appear in the low - hundreds per domain based on published tables and analyses (this characterization is qualitative). By comparison, 30 scenarios is modest but defensible if: Each scenario is multi - turn and includes multiple tools and decision points (e.g., multiple calls per mission design). The scenario set is carefully stratified to cover distinct error modes: correct numeric results, out - of - catalog bodies, invalid trajectories, ambiguous inputs, etc. Each scenario yields multiple graded dimensions (numeric fidelity, refusal fidelity, provenance, uncertainty/assumption handling), so your effective sample size per dimension is larger (e.g., 30 scenarios × 3–5 scored decisions each). ToolEmu shows that even a moderate number of well - designed scenarios can reveal significant risk patterns; their value comes from targeted coverage of risky behaviors, not sheer size. Similarly, τ - bench's contribution is in its realistic airline/retail modeling rather than only scale. Design implication: With 30 carefully structured, multi - step astrodynamics scenarios, you can make a credible, clearly differentiated contribution if you: (a) explicitly map each scenario to one or more faithfulness dimensions, (b) show per - dimension statistics, and (c) position the benchmark as "depth on scientific honesty" rather than sheer breadth. 

L1.3: How should you formalize "faithful transmission" into quantitative metrics? 

Why this matters: Without clear, quantitative definitions, you can't compare models or argue that you're filling a precise gap. Answer (

L1.3): Drawing from how BFCL formalizes function correctness via AST matching, τ - bench defines pass^k on task success, and RAG attribution work operationalizes "faithfulness" as consistency between answer spans and evidence spans, you can define four concrete, programmatically checkable metrics: Numeric Fidelity Score (NFS): For each tool - returned scalar or vector, check whether the final answer's values are within a specified tolerance of the tool output. Score per decision: 1 if all reported numbers match tool outputs within tolerance, 0 otherwise; aggregate as mean over decisions. 

Refusal Fidelity Rate (RFR): For scenarios where the tool returns a typed refusal (not_found, insufficient_data, etc.), classify the agent's response as: Faithful refusal (clearly says it cannot answer due to tool limitations, no fabricated numbers). Partial fidelity (mentions limitations but also speculates or fabricates). Fabricated (ignores/refutes tool refusal and gives confident answer). Programmatically: you can tag expected refusal cases and use string/structure checks plus minimal human/LLM review to label categories. RFR is the fraction in the "faithful refusal" bucket. 

Provenance Transmission Accuracy (PTA): Given the evidence envelope's provenance fields (e.g., tool name, artifact ID, commit hash, dataset version), check whether the agent's answer mentions the correct provenance tokens (or acceptable short forms). PTA per decision: 1 if correct provenance is present and no incorrect provenance is injected; 0 otherwise. 

Assumption & Uncertainty Preservation (AUP): For tool outputs with explicit assumptions (e.g., reference frames, time epochs, perturbation models) and confidence/validity intervals, check whether the answer: Mentions key assumptions (e.g., "assuming two - body dynamics," "valid for launch dates between X and Y"). Does not implicitly contradict them (e.g., claiming results outside validity interval). AUP per decision: 1 if all required assumptions/validity conditions are appropriately conveyed; 0 otherwise. These metrics align with existing benchmark philosophy (clear per - example pass/fail or similarity criteria) but extend it from tool - call correctness to evidence - to - answer correctness. Design implication: You can pre - register these four metrics as your primary outcomes, giving your study a crisp, quantitative contribution. 

LEVEL 2 FOLLOW - UPS Now, Level - 2 for the Level - 1 answers that materially affect design. I'll pick 

L1.1 (grading method) and 

L1.3 (metric definition) as the most impactful. 

L2.1 (from 

L1.1): Is there any role for LLM - as - judge in an honesty benchmark that already has deterministic grading? 

Why this matters: You need to decide whether to introduce additional complexity/noise for secondary signals like explanation quality or borderline cases. Answer (

L2.1): ToolBench and ToolEmu show that LLM - as - judge is mainly useful when outcomes cannot be cleanly expressed as deterministic predicates—for example, when assessing nuanced natural - language quality, user satisfaction, or risk categories. ToolBench relies on ChatGPT judgments precisely because pass/fail cannot always be determined programmatically; human agreement is high but not perfect. In your case, core honesty metrics can and should be fully programmatic, as with BFCL and τ - bench. However, there are at least two plausible roles for an LLM - judge: Secondary "user - perceived honesty" metric: Ask an external LLM to read tool outputs and the agent answer, then categorize whether the answer appears honest, hedged, or misleading. This could expose subtle misframings that your strict metrics may miss (e.g., selective omission of caveats). Adjudication of ambiguous cases: For borderline cases where simple string/tolerance checks fail (e.g., paraphrased provenance, partial assumption coverage), an LLM - judge could provide tie - breaking labels. To preserve rigor and reproducibility, follow BFCL's lead and: Treat LLM - judged signals as secondary, exploratory metrics, not primary; Fix the judging model and prompt at pre - registration time; Possibly validate a subset against human labels, as ToolBench did with its 87.1%/80.3% agreement figures. Design implication: Use LLM - as - judge sparingly, in a pre - registered and clearly secondary role—primarily programmatic grading with LLM - judged "soft honesty" as an auxiliary lens. 

L2.2 (from 

L1.3): How granular should the metrics be—per scenario, per tool call, or per "evidence - carrying decision"? 

Why this matters: Granularity affects statistical power, interpretability, and how much the study tells you about specific failure modes vs. aggregate behavior. Answer (

L2.2): Existing benchmarks suggest granularity aligned with atomic decisions: BFCL scores at the level of individual function calls (each Q–function–answer triple). τ - bench scores at the level of end - to - end trajectories (per task) using pass^k, but internal analyses often break down errors by step type (tool choice, parameterization, rule application). RAG attribution benchmarks typically score at the level of spans or question–answer pairs, which correspond to "evidence - bearing decisions." For your benchmark, a natural unit is the "evidence - carrying decision": each time the agent chooses to state a numerical result, cite a source, or accept/refuse an answer based on a tool call. A single scenario (e.g., multi - leg mission design) might contain multiple such decisions (e.g., initial transfer, mid - course correction, return leg feasibility). Scoring per decision gives you more data points (e.g., 30 scenarios × ~5 decisions = 150 data points per metric) without needing hundreds of scenarios. Aggregation can then be: Per - scenario (e.g., average NFS across decisions in that scenario). Per - model (mean over all decisions across scenarios). Per - dimension (NFS, RFR, PTA, AUP) to reveal which aspects of honesty each model struggles with. Design implication: Pre - register the decision - level as your primary unit of analysis, with scenario - level summaries for readability; this maximizes informational yield from a 30 - scenario budget. 

LEVEL 3 FOLLOW - UP One Level - 3 follow - up remains high - impact: a deeper question off 

L2.2 about balancing numeric vs. categorical metrics. 

L3.1 (from 

L2.2): Should you use continuous scores (e.g., calibrated distances, partial credit) or strict binary labels for each faithfulness metric? 

Why this matters: This choice affects sensitivity to subtle improvements (e.g., better uncertainty wording) and statistical analysis. Answer (

L3.1): Existing benchmarks mostly use binary or categorical labels at the atomic level, aggregating them into averages: BFCL uses a correctness score per function call (correct/incorrect via AST matching), then averages to get accuracy. τ - bench uses pass/fail per trajectory to compute pass^k. ToolBench's LLM - judge returns pass/win labels (with probabilities internally) that are reported as aggregate rates. RAG attribution work sometimes uses graded similarity scores (e.g., token - level overlaps or graded faithfulness), but these are often harder to interpret and validate. For your metrics: Numeric Fidelity lends itself to a binary metric (within tolerance vs. not) with a pre - registered tolerance grounded in astrodynamics practice (e.g., relative error thresholds), plus possible secondary analysis on error magnitude distribution. Refusal Fidelity, Provenance Transmission, and Assumption/Uncertainty Preservation are naturally categorical (faithful / partial / unfaithful) and can be reduced to binary for primary analysis (faithful vs. non - faithful) or kept multi - class for error taxonomy. Binary/categorical labels: Make pre - registered analysis straightforward and robust. Mirror current benchmark practice, helping others interpret your results. Still allow richer post - hoc analysis (e.g., distribution of numeric errors, breakdown of partial fidelity types). Design implication: Use binary or small categorical labels as primary metrics, with continuous measures (e.g., numeric error magnitudes) as secondary analyses; this keeps the benchmark interpretable and aligned with existing evaluation practice. LOAD - BEARING NUMBERS Below is a flat list of every number that could influence design decisions, with source and whether it is official - published vs. third - party - estimated: BFCL includes 100 Java function - calling cases (official - published). BFCL includes 50 JavaScript function - calling cases (official - published). BFCL includes 70 REST API function - calling cases (official - published). BFCL includes 100 SQL function - calling cases (official - published). BFCL includes 1,680 Python function - calling cases (official - published). BFCL - Live comprises more than 67,000 real - life function - calling examples (official - published). BFCL evaluation package version bfcl-eval==2025.12.17 corresponds to leaderboard commit f7cf735 as of 2026 - 04 - 12 (official - published). ToolBench uses 16,464 real - world REST APIs from RapidAPI (official - published). ToolBench covers 49 API categories (official - published). ToolBench contains 126,486 instruction instances (official - published). ToolBench includes 469,585 actual API calls (official - published). ToolBench averages 4.0 reasoning traces per instruction instance (official - published). ToolBench reports GPT - 4 with DFSDT achieves 71.1% pass rate (official - published). ToolBench reports GPT - 4 with DFSDT achieves 70.4% win rate (official - published). ToolBench reports ToolLLaMA - 2 - 7B - v2 reaches 66.7% pass rate (official - published). ToolBench reports 87.1% human agreement on pass rate with ChatGPT - as - judge (official - published). ToolBench reports 80.3% human agreement on win rate with ChatGPT - as - judge (official - published). τ - bench retail results for claude - 3.5 - sonnet - 20241022: pass^1 = 0.692 (official - published). τ - bench retail results for claude - 3.5 - sonnet - 20241022: pass^2 = 0.576 (official - published). τ - bench retail results for claude - 3.5 - sonnet - 20241022: pass^3 = 0.509 (official - published). τ - bench retail results for claude - 3.5 - sonnet - 20241022: pass^4 = 0.462 (official - published). τ - bench airline results show gpt - 4o - mini pass^1 = 0.225, pass^2 = 0.140, pass^3 = 0.110, pass^4 = 0.100 as of 2024 - 07 - 18 (official - published). IRMA framework on τ - bench improves overall pass^5 scores by 16.1% over ReAct (official - published). IRMA framework on τ - bench improves overall pass^5 scores by 12.7% over Function Calling (official - published). IRMA framework on τ - bench improves overall pass^5 scores by 19.1% over Self - Reflection (official - published). AgentBench core benchmark has 1,091 tasks across 8 environments (third - party - estimated from benchmark summaries, not stated as a single number in the original paper). AgentBench composite score for GPT - 4 reported as 4.27 (third - party - estimated summary). AgentBench composite score for Llama - 2 - 13B - chat reported as approximately 1.01 (third - party - estimated summary). ToolEmu reports that 68.8% of failures identified with ToolEmu would be valid according to human evaluation (official - published). Your planned study scale: 30 scenarios (study - design choice, not from a cited paper; listed here because it affects statistical power and design). Is there a particular dimension (numeric fidelity, refusal handling, provenance, or assumptions/uncertainty) you'd most like to optimize the benchmark for, so we can next translate these metrics into concrete scenario templates for aster - mission - mcp?
